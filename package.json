{
  "name": "optionsdashboard-convex",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "convex dev",
    "deploy:prod": "convex deploy --prod",
    "deploy:preview": "convex deploy --preview-name preview"
  },
  "dependencies": {
    "@convex-dev/auth": "^0.0.90",
    "convex": "^1.17.4"
  }
}
